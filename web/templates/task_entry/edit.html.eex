<h2>Edit Task Entry</h2>

<%= render "form.html", changeset: @changeset,
                        action: customer_project_task_task_entry_path(@conn, :update, @customer, @project, @task, @task_entry) %>
